#include<stdbool.h>
#include<string.h>

bool doesAliceWin(char*s){
    int vowelCount=0;

    for(int i=0;s[i]!='\0';++i){
        char c=s[i];
        if(c=='a'||c=='e'||c=='i'||c=='o'||c=='u'){
            vowelCount++;
        }
    }
    return vowelCount>0;
}
